export const grader = (input) => {
  // console.log(input);
  // for(let i = 0; i < input.length; i++){
  //   input[i].ideal = true;
  // }
  input[2].ideal = true;
  input[4].ideal = true;
  return input;
}